#!/usr/bin/env bash
for i in $(find . -type d | grep -v .svn); do
  X=$(svn pg svn:externals "$i" 2>/dev/null | egrep "branches|tags")
  if [ "$X" ]; then
    echo "=== $i ==="
    echo "$X" | egrep --color=auto "branches|tags"
    echo
  fi
done
